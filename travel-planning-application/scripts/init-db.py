import json
import os

# This script initializes the database by running the SQL scripts
# The SQL scripts will be executed when the app first loads

print("[v0] Database initialization scripts are ready")
print("[v0] The database will be created and seeded when you first run the app")
print("[v0] SQL scripts location: scripts/001-initial-schema.sql and scripts/002-seed-data.sql")
